{
  "name": "chatbot-backend",
  "version": "1.0.0",
  "description": "RAG Chatbot Backend for Online Learning Platform",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "ingest": "node -e \"import('./chatbotBackend.js').then(m => { const chatbot = new m.default(); chatbot._ensurePgVectorExtension().then(() => chatbot.ingestContentEmbeddings()).then(() => chatbot.close()); })\"",
    "test": "node test.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
